import * as React from "react";
import "antd/lib/select/style";
import Select from "antd/lib/select/index";
if (a === Select) {
}
if (Select) {
}
Select ? "a" : "b";
a ? Select : 2;
Select || "a";
a || Select;
